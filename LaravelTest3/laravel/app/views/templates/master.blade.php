<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>Laravel Test3 About Page</title>
    </head>
    <body>
        <div class="welcome">Laravel Test3 About Page



            <?php
            /*
              <?= $greeting. ' ' . $thing =>

              @foreach ($items as $item)
              {{$item.' '}}
              @endforeach


              @if(count($items))
              {{'Items exists'}}
              @endif


              @forelse ($items as $item)
                {{$item.' '}}
              @empty // @else
                {{'There are no items'}}
              @endforelse


              /* */
            ?>
        </div>
        <div class="content">
            @yield('content')
        </div>
    </body>
</html>
